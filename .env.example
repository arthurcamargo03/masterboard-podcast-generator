# Chave da API de IA. Se ausente, o app roda em modo MOCK automaticamente.
AI_API_KEY=

# Modelo usado na chamada (ex: claude-sonnet-5, gpt-4o, etc). Configurável, não hardcoded.
AI_MODEL=claude-sonnet-5
